import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-radni-nalog',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
